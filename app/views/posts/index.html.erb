<h2 class="pd_top">Murmurs</h2>

<p><%= notice %></p>

<div class="row justify-content-center">
  <table>
    <tr>
      <th>Recent murmurs.</th>
    </tr>
    <% @posts.each do |post| %>
    <tr>
      <td class ="col-lg-3"><%= post.content %></td>
      <td class ="col-lg-2"><%= time_ago_in_words(post.created_at) %></td>
      <td class ="col"><%= link_to 'Check your Murmur.', post_path(post.id), class: "btn btn-outline-success" %></td>
      <td class ="col"><%= link_to 'Edit your Murmur.', edit_post_path(post.id), class: "btn btn-outline-primary" %></td>
      <td class ="col"><%= link_to 'Derete your Murmur.', post_path(post.id), method: :delete, class: "btn btn-outline-secondary", data: { confirm: 'Are you sure you want to delete it?' } %></td>
    </tr>
    <% end %>
  </table>
</div>

<h4 class="pd_btm"><%= link_to %q{Let's post your new Murmur!}, new_post_path %></h4>